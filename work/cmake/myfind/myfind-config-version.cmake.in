set(PACKAGE_VERSION "@MYFIND_VERSION@")

if("${PACKAGE_FIND_VERSION}" STREQUAL "@MYFIND_VERSION@")
  set(PACKAGE_VERSION_EXACT 1)
  return()
else()
  set(PACKAGE_VERSION_COMPATIBLE 1)
  return()
endif()

